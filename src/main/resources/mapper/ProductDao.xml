<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bs.flower.dao.ProductDao">

    <resultMap type="com.bs.flower.entity.Product" id="ProductMap">
        <result property="productId" column="product_id" jdbcType="INTEGER"/>
        <result property="productName" column="product_name" jdbcType="VARCHAR"/>
        <result property="primaryPrice" column="primary_price" jdbcType="OTHER"/>
        <result property="nowPrice" column="now_price" jdbcType="OTHER"/>
        <result property="amout" column="amout" jdbcType="INTEGER"/>
        <result property="path" column="path" jdbcType="VARCHAR"/>
        <result property="classfy" column="classfy" jdbcType="INTEGER"/>
        <result property="color" column="color" jdbcType="VARCHAR"/>
        <result property="desc" column="desc" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateItme" column="update_itme" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ProductMap">
        select
          product_id, product_name, primary_price, now_price, amout, path, classfy, color, descr, create_time, update_itme
        from flower.product
        where product_id = #{productId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ProductMap">
        select
          product_id, product_name, primary_price, now_price, amout, path, classfy, color, descr, create_time, update_itme
        from flower.product
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="ProductMap">
        select
          product_id, product_name, primary_price, now_price, amout, path, classfy, color, descr, create_time, update_itme
        from flower.product
        <where>
            <if test="productId != null">
                and product_id = #{productId}
            </if>
            <if test="productName != null and productName != ''">
                and product_name = #{productName}
            </if>
            <if test="primaryPrice != null">
                and primary_price = #{primaryPrice}
            </if>
            <if test="nowPrice != null">
                and now_price = #{nowPrice}
            </if>
            <if test="amout != null">
                and amout = #{amout}
            </if>
            <if test="path != null and path != ''">
                and path = #{path}
            </if>
            <if test="classfy != null">
                and classfy = #{classfy}
            </if>
            <if test="color != null and color != ''">
                and color = #{color}
            </if>
            <if test="desc != null and desc != ''">
                and desc = #{desc}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateItme != null">
                and update_itme = #{updateItme}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="productId" useGeneratedKeys="true">
        insert into flower.product(product_name, primary_price, now_price, amout, path, classfy, color, descr, create_time, update_itme)
        values (#{productName}, #{primaryPrice}, #{nowPrice}, #{amout}, #{path}, #{classfy}, #{color}, #{desc}, #{createTime}, #{updateItme})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update flower.product
        <set>
            <if test="productName != null and productName != ''">
                product_name = #{productName},
            </if>
            <if test="primaryPrice != null">
                primary_price = #{primaryPrice},
            </if>
            <if test="nowPrice != null">
                now_price = #{nowPrice},
            </if>
            <if test="amout != null">
                amout = #{amout},
            </if>
            <if test="path != null and path != ''">
                path = #{path},
            </if>
            <if test="classfy != null">
                classfy = #{classfy},
            </if>
            <if test="color != null and color != ''">
                color = #{color},
            </if>
            <if test="descr != null and descr != ''">
                descr = #{desc},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateItme != null">
                update_itme = #{updateItme},
            </if>
        </set>
        where product_id = #{productId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from flower.product where product_id = #{productId}
    </delete>

</mapper>